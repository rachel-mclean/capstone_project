{{#if showButtons}}
  <div id="home-page">
    <div class="container">
      <div class="text-center">
        <h1>The Know-How Swap</h1>
          <h2>Teach an old skill. Learn a new skill. Connect.</h2>
            <a style="width: fit-content; margin: 2% auto;" href="/sign-in" class="btn btn-primary btn-lg">Log in</a>
            <a style="width: fit-content; margin: 2% auto;" href="/sign-up" class="btn btn-primary btn-lg">Make an account</a>
      </div>
    </div>
  </div>
{{/if}}
  {{!-- <h2>Welcome!</h2>

  <p>
    You're logged in as <a href="mailto:{{user.email}}">{{user.email}}</a></span>.
  </p>

  <form class="form-signout" action="/sign-out" method="POST">
    <button class="btn btn-large btn-danger" type="submit">Logout</button>
  </form> --}}
  </div>
</div>
